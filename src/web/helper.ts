export default function (effect, setApp, setContent) {
    /*effect(() => {
        FireJSX.runApp = (App, Content) => {
            setApp(App);
            setContent(Content);
        }
    }, [])*/
}
