import {describe} from 'mocha';
import {expect} from 'chai';

describe('FireJSX', function () {
    describe('utils', function () {
        const {convertPathToUrl, rmIndexSuffixFromPath} = require('../dist/utils/LinkTools');
        expect('a')
    })
});